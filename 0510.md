
# 2026-05-10 机试

开始之前, 请将手机调至静音, 并配置好开发环境.


## 基本规则

1. 使用 git 管理代码仓库
2. 编码时长三个小时
3. 不限制编程语言
4. 推荐使用AI辅助生成代码
5. 三个问题只需要解决其中的一个
6. 完成之后将整个 git 代码库压缩, 存储为 "姓名.zip", 并提交


## 问题一, 实现文件上传服务, 60 分

要求:
1. 基于 HTTP 协议
2. 支持断点传
3. 如果服务端已经存在相同文件, 不需要重复上传, 实现秒传的效果
4. 要编写单元测试
5. 不限制使用的数据库类型, 比如 postgresql/sqlite 等都可以
6. 提供项目设计文档

可选的模块:
1. 实现一个 CLI 工具, 可以通过命令行上传大文件到服务端
2. 实现一个 web 前端页面, 可以在浏览器里上传大文件到服务端
3. 编写 Dockerfile/docker-compose.yml, 方便部署该服务



## 问题二, 自定义 agent, 90 分

claude code, qoder, open claw 等 agent 工具, 都包含了类似的 agent loop 模块.
你的任务是实现一个 agent loop, 要求:
1. 提供 CLI, 可以调用这个 agent loop, 通过 CLI 与大模型交互
2. tools 模块, 自定义工具扩展, 能实现基本的文件操作, 能调用命令行工具
3. 提供项目设计文档

可选的模块:
1. 支持 vibe coding
2. providers 模块, 管理大模型服务商
3. mcp 模块, 支持 mcp 扩展
4. skills 模块, 支持 skill 扩展


## 问题三, 扩展 git 存储后端, 100 分

git 作为最流行的版本管理工具, 通常存储的都是文本文件, 存储/压缩/diff 文本文件时都很容易;
后来为了支持存储较大的二进制文件, 社区开发了LFS (Git Large File Storage) 扩展.

但是, 这个扩展判断二进制文件是否发生了改变, 是基于文件哈稀算法.
一个1GB的大文件即使只发生了一个字节的变化, 其哈稀值也会变化,
git 就会重新将其存储到 git object 里, 存储该文件的两个版本, 就要占用1GB + 1GB 的存储空间.
这种做法的好处是实现逻辑很简单, 但缺点也很明显, 会重复存储大量的数据, 导致整个 git 仓库存储空间极速膨胀.

你的任务是修改/或者重新实现一个新的 git 存储后端, 针对二进制文件, 利用文件分片的方式来存储:
1. 以 64KB 为单位将一个大文件(比如1GB)分隔成多个片段, 分别存储
2. 比如当该制文件的头部几个字节被修改了, 只需要重新存储第一个片段(64KB), 后面的都保持不变
3. 这样以来, 存储该大文件的两个版本, 只需要占用1GB + 64KB 的存储空间

要求:
1. 只能修改 git 存储后端逻辑, 保持 git 前端操作/命令行选项不变
2. 可以模仿或者参考 git lfs 扩展

因为时间有限, 可能来不及调试修复问题, 只需要基本实现这个功能即可.


